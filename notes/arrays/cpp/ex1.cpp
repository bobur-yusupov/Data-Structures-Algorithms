#include <bits/stdc++.h>
using namespace std;

int main() {
    // Create vector of 5 elements
    vector<int> v = {1, 2, 3, 4, 5};

    for (int i = 0; i < v.size(); i++) {
        cout << v[i] << " ";
    }

    return 0;
}
