from typing import List  # List type is used to specify the data structure of variables

sample_list: List[int] = [1, 5, 9, 2]  # sample_list contains integer elements

first_element_value = sample_list[0]  # Access the first element
length_of_list = len(sample_list)  # Use the built-in function to get the length of the list
last_element_value = sample_list[-1]  # Access the last element using -1 index

print(first_element_value)  # 1
print(last_element_value)    # 2
